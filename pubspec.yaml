name: quickqash
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  esc_pos_utils_plus: ^2.0.4
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^17.0.1
  intl: ^0.20.2
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  network_info_plus: ^7.0.0
  path_provider: ^2.1.5
  pdf: ^3.11.3
  printing: ^5.14.2
  uuid: ^4.5.2
  # Bluetooth thermal printer support
  blue_thermal_printer: ^1.2.3
  # USB thermal printer support
  usb_serial: ^0.5.1
  # Runtime permissions for Android Bluetooth/Location
  permission_handler: ^11.3.1
  # Optional: Sunmi device built-in printer support
  sunmi_printer_plus: ^2.0.5

dev_dependencies:
  build_runner: any
  isar_generator: any
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
